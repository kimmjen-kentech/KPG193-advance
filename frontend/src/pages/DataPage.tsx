import { Database } from 'lucide-react';
import { StubPage } from './StubPage';

export const DataPage = () => (
  <StubPage
    title="Data Registry"
    tagline="MATPOWER case · CSV profiles · reference UC"
    icon={Database}
  />
);
