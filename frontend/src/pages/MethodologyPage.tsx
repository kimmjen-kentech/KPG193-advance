import { BookOpen } from 'lucide-react';
import { StubPage } from './StubPage';

export const MethodologyPage = () => (
  <StubPage
    title="Methodology"
    tagline="Spatial clustering · MATPOWER v2 · KEPCO districts"
    icon={BookOpen}
  />
);
