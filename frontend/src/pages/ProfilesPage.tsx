import { Activity } from 'lucide-react';
import { StubPage } from './StubPage';

export const ProfilesPage = () => (
  <StubPage
    title="Temporal Profiles"
    tagline="Hourly demand · renewable factors · weather"
    icon={Activity}
  />
);
